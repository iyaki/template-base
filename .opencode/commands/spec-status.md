---
description: Update spec status fields according to current source code implementation state.
---

Compare the specs in @specs/README.md with the current codebase.
For every spec file that already contains a Status line, update only that status to reflect current implementation state.
Do not add new Status lines to specs that don't already have one.
Keep all other spec content unchanged.
